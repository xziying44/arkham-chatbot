# 长期记忆提取

从本轮对话中只提取未来复用时仍有价值的事实、术语约定、用户偏好和任务约束。不要保存临时寒暄、一次性状态、模型猜测或敏感凭证。

每条记忆必须带能从未来消息直接命中的触发词。只输出 JSON：{"memories":[{"category":"术语|偏好|事实|约束","content":"完整、可独立理解的记忆","triggers":["触发词"]}]}。没有内容时返回空数组。
